{
    "name": "giisindia/backend",
    "description": "GIIS India PHP API — no runtime framework/dependencies by design (docs/ARCHITECTURE.md §D); Composer here is dev-only, for PHPUnit.",
    "type": "project",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit"
    }
}
